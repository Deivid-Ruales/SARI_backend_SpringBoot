package com.deivid.SpringProject.modelo;

// Define el enum Rol, que contiene los roles posibles para un usuario
public enum Rol {
    Administrador,  // Rol para usuarios con privilegios administrativos
    Cliente,        // Rol para usuarios que son clientes
    Empleado        // Rol para usuarios que son empleados
}
